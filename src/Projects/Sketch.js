import React from 'react';
import {Link} from 'react-router-dom';

const Sketch = ({match}) => (
    <div>
        <Link to={`/projects`}>
            back
        </Link>
        <div id="wrapper">
            
        </div>
    </div>
)

export default Sketch;