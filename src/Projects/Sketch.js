import React from 'react';
import {Link} from 'react-router-dom';

const Sketch = () => (
    <div>
        PROGETTO
        <Link to={`/projects`}>
            back
        </Link>
    </div>
)

export default Sketch;